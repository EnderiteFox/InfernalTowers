package api;

/**
 * An interface representing the game handler for the infernal towers game
 */
public interface InfernalTowerGame {
    /**
     * Starts the game loop. Returns once the game is finished
     */
    void startGame();
}
